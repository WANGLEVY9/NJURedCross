# 南京大学红十字会平台 — 网站架构、功能与数据连通说明

> 本文档描述平台的实际运行形态，以及各功能模块与南大表格（SeaTable）底表之间的读写对应关系。
> 所有表名、列名、行数与关联关系均经**实测核对**（读取运行中的服务与两个 Base 的真实元数据），不是设计稿推断。
> 核对时间：2026-09-16。核对方式见文末「附录 A」。

---

## 1. 总体架构

### 1.1 四层结构

平台是一个**单进程 Node 服务 + 双界面 + 两个 SeaTable Base**的结构。**所有业务状态都存放在 SeaTable**，服务端不再向本地文件系统写入任何状态：

```
┌─ 界面层（浏览器，原生 ES Module，无构建工具）────────────────────┐
│  公众服务门户  /                    运营管理控制台  /console/**   │
│  └ 共享：core（dom/router/api/store/motion/format/toast/keys）    │
│           ui（primitives/overlay/palette/chart/table）           │
└──────────────────────────┬───────────────────────────────────────┘
                           │ 同源 fetch（Cookie 会话 + CSRF）
┌──────────────────────────▼───────────────────────────────────────┐
│  服务层  server.js（单进程，无状态）                              │
│  静态资源 + CSP ｜ /api/public/* ｜ /api/portal/* ｜ /api/auth/*  │
│  ｜ /api/*（控制台，仅 platform_admin）                           │
│  限流 · 域名白名单 · 字段脱敏 · 业务状态机 · 审计写入             │
└───────────────────────────┬──────────────────────────────────────┘
                            │ SeaTable SDK（服务端持有 Token）
        ┌───────────────────┴───────────────────┐
┌───────▼────────────────────┐      ┌───────────▼──────────────────┐
│ 主 Base  25 张表           │      │ 志愿服务 Base  23 张表        │
│ 19 业务表 + 6 平台状态表   │      │ 只读，绝无写入                │
│ 读写                       │      │                              │
└────────────────────────────┘      └──────────────────────────────┘
```

> **服务进程不持有任何状态。** 任意数量的实例可以并行运行并共享同一份数据 —— 这是本次把 `logs/*.json` 迁入 SeaTable 的核心收益（见 §2.3）。
> 唯一例外是库存串行锁 `withMaterialLock()` 与登录失败计数，二者仍是**进程内**的内存结构（见 §5.11）。

### 1.2 一次请求的完整链路

```
浏览器 → fetch('/api/...')
  → server.js http.createServer
  → /api/ 前缀 → api()
      ├ /api/public/*  → publicRoutes()   （无会话；限流 + 同意校验 + 域名白名单）
      ├ /api/auth/*    → authApi()        （登录/登出/会话探测）
      └ 其余           → requireSession() → 写操作再过 requireWriteAccess()（CSRF）
                        → SeaTable SDK（服务端持有 Token）
                        → 投影/脱敏（maskedApplicant、maskedEmail、字段白名单）
                        → recordAudit() 向 操作审计表 追加一行
  → 非 /api/ 前缀 → staticFile()
      ├ 命中磁盘文件 → 200 + ETag（无后缀未命中则回退 index.html，交给前端路由）
      └ 无后缀路径   → sendAppShell() 返回 index.html
```

**关键点：浏览器永远拿不到 SeaTable 凭据。** Token 只存在于服务端 `.env`；服务端在返回前完成字段白名单与脱敏，公众端投影（`publicEventProjection`）根本不包含任何个人信息。

### 1.3 前端技术形态与硬约束

| 项目 | 现状 |
| --- | --- |
| 构建工具 | **无**。`public/app/**` 直接以原生 ESM 由浏览器加载 |
| 运行时依赖 | **无**（图表、动画、抽屉、命令面板全部自研） |
| CSP | `default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data:` |
| 由此产生的约束 | **代码中不存在内联脚本与内联样式**；所有动态样式只能走 CSSOM（`style.setProperty`）或 SVG 表现属性 |
| 主题 | 双表面同名语义变量：公众端暖纸质浅色，管理端中性深色（`tokens.css` 为唯一来源） |
| 路由 | `app/main.js` 定义路由表，按 `surface` 惰性加载 shell 与页面模块 |
| 服务端路由回退 | 无扩展名路径一律返回 `index.html`，由前端路由接管 |

### 1.4 服务端结构（server.js）

| 区块 | 职责 |
| --- | --- |
| `securityHeaders()` | CSP、`X-Frame-Options: DENY`、`nosniff`、`no-referrer`、`Permissions-Policy` |
| `recordAudit()` | 向 `操作审计表` 追加一行；**不记录** Token、密码、签到码明文。失败只记 console.error，绝不阻断业务操作 |
| `stateRows()` / `saveEnrollment()` / `writeSubmissionReview()` 等 | 平台状态层读写（见 §2.3）。**读失败会抛错**，不再静默降级为空结果 |
| `enforcePublicLimit()` | 门户写接口按「IP + 动作分桶」每小时限流 |
| `assertPublicEmail()` | 校验邮箱后缀属于 `PUBLIC_EMAIL_DOMAINS`（默认 `nju.edu.cn`、`smail.nju.edu.cn`） |
| `getMaterialsOverview()` 等 | 各业务模块的聚合与投影 |
| `withMaterialLock()` | 按资产编码串行的库存锁（**仅覆盖单个 Node 进程**） |
| `api()` / `portalRoutes()` / `publicRoutes()` / `authApi()` | 四层路由分发：控制台（`platform_admin`）/ 门户个人中心（任意角色）/ 门户公开与写入 / 登录 |
| `requirePortalSession()` / `requirePortalWrite()` / `requireConsoleAccess()` / `requireCsrf()` | 三类守卫 + CSRF 校验，边界见 §1.5 |
| `loadAccounts()` | 启动时加载并校验账号文件；任一项不合法即 `process.exit(1)`，不做部分加载 |
| `staticFile()` | 静态资源 + ETag 协商缓存（资源未版本化，故用 `no-cache` 而非固定长缓存） |

### 1.5 鉴权与写入保护

会话与守卫：

- 会话：`nju_redcross_session`，**HMAC-SHA256 签名**的 base64url payload，`HttpOnly` + `SameSite=Strict`，默认 8 小时（`PLATFORM_SESSION_TTL_HOURS`，范围 1h–7d）。两个界面**共用同一个会话 Cookie**，角色决定能进入哪个界面。
- 写操作：`POST/PUT/DELETE` 必须携带与会话绑定的 `X-CSRF-Token`，用 `timingSafeEqual` 比对。CSRF 校验**绑定动词而非请求体**——无 body 的 `DELETE` 同样需要令牌。
- 登录失败：按 IP **15 分钟内 5 次**封禁。
- 前端在会话过期时自动跳转：控制台走 `/console/login`，门户走 `/login`，均带 `?next=<原地址>`；令牌失效时自动刷新一次并重放请求。

账号与角色（`.platform-accounts.json`，口径见 §2.4）：

| 角色 | 可访问界面 | 说明 |
| --- | --- | --- |
| `platform_admin` | 控制台 + 门户 | 管理平台账号，可用全部控制台 API |
| `member` | 仅门户 | 活动平台账号，读控制台接口一律 403 |

守卫实现为三个函数，边界清晰且互不重叠：

| 守卫 | 允许 | 拒绝时的响应 |
| --- | --- | --- |
| `requirePortalSession` | 任意已登录账号 | `401 login_required` |
| `requirePortalWrite` | 任意已登录账号 + CSRF | `401 login_required` / `403 csrf_failed` |
| `requireConsoleAccess` | 仅 `platform_admin` | `401 login_required` / `403 console_forbidden` |

**门户的分级鉴权**：读接口保持公开（活动列表与详情、物资说明、概览、温暖连接介绍），因此访客可以先浏览再决定参加；写接口与个人中心需要登录——报名、物资借用、投稿、温暖连接登记以及 `/api/portal/me` 都要求账号身份。这样做的直接收益是记录可归属：报名写入 `活动报名表.参与者引用`、投稿写入 `宣传投稿表.投稿人引用`、温暖连接登记写入 `温暖连接参加表.参与者标识`，个人中心据此只返回调用者自己的记录，接口不接受任何形式的他人标识。

口令策略：开发环境下限 6 位，生产环境 16 位，可用 `PLATFORM_ACCOUNT_MIN_PASSWORD_LENGTH` 覆盖（但不允许低于 6）。非生产环境启动时会打印短口令告警，避免"本地能跑、上线被拒"的意外。当前账号口令**仍为明文比对**，生产部署前必须迁移到哈希存储。

---

## 2. 数据源总览

### 2.1 主 Base（读写）

- 地址：`https://table.nju.edu.cn`
- `dtable_uuid`：`c707b1ab-ac5c-46b3-9d77-1bc4ea16f241`
- Token 名称：`连接测试`（对应 `.env` 的 `SEATABLE_API_TOKEN`）
- **共 25 张表** = 原有 19 张业务表 + 本次新增的 6 张平台状态表。
  19 张业务表中平台使用 10 张（含 3 张遗留表），其余 9 张仅通过「数据中心」通用 CRUD 可达。

**平台状态表（本次新增，6 张）**

这 6 张表取代了原先的 `logs/*.json`，使平台状态与业务数据落在同一个 Base 里，共享同一套鉴权、备份与 UI 浏览能力。由 `npm run state:apply` 创建（见 §7）。

| 表名 | 列数 | 取代的原文件 | 承载业务 |
| --- | ---: | --- | --- |
| 宣传项目表 | 10 | —（原设计声明但从未创建） | 宣传主题、征集窗口、受众与发布渠道 |
| 宣传投稿表 | 20 | `public-submissions.json` + `outreach-reviews.json` | 公众端投稿正文与授权；以及对遗留内容（策划案/文创/试课）的审核结论，按 `来源` 列区分 |
| 宣传发布任务表 | 13 | `outreach-publications.json` | 渠道排期、发布确认、链接与失败重试记录 |
| 温暖连接参加表 | 16 | `warmth-interest.json` + `community-consent.json` | 公众端自愿登记与控制台侧同意记录，按 `来源` 列区分 |
| 温暖连接投稿表 | 11 | `community-submissions.json` | 生日祝福与早安晚安投稿及审核结论 |
| 操作审计表 | 9 | `audit.jsonl` | 登录、审批、出入库、签到核验、内容审核、公众端提交的操作留痕 |

**原有业务表（19 张）**

| # | 表名 | 列数 | 行数 | 平台角色 |
| --- | --- | ---: | ---: | --- |
| 1 | 物资管理 | 23 | 32 | **业务写入**（借用申请、审批、出库归还） |
| 2 | 工位物资表 | 11 | 140 | **只读**（库存底账 ‖ 平台仅读到 100 行，见 §5.1） |
| 3 | 物资流水表 | 14 | 0 | **业务写入**（出入库台账）**当前为空** |
| 4 | 物资配置表 | 9 | 100 | **只读**（预警阈值、资产编码映射） |
| 5 | 活动项目表 | 14 | 1 | **业务写入**（活动创建/发布/关闭） |
| 6 | 活动场次表 | 9 | 0 | **业务写入**（场次创建） |
| 7 | 活动报名表 | 15 | 0 | **业务写入**（报名、取消、签到） |
| 8 | 博爱青春策划案 线下答辩 | 8 | 12 | **只读**（宣传中心内容源，遗留表） |
| 9 | 博爱青春纪念品大赛 | 9 | 18 | **只读**（宣传中心内容源，遗留表） |
| 10 | “红十字生命教育＋”第一轮试课 | 9 | 10 | **只读**（宣传中心内容源，遗留表） |
| 11 | 全体成员表（26~27学年） | 18 | — | 仅数据中心可达 |
| 12 | 博爱青春队长团 | 7 | — | 仅数据中心可达 |
| 13 | 工位值班表 | 8 | — | 仅数据中心可达 |
| 14 | 换届意向统计 | 7 | — | 仅数据中心可达 |
| 15 | 换届报名表 | 28 | — | 仅数据中心可达（含个人信息） |
| 16 | 8月5日测试 | 7 | — | 仅数据中心可达（疑似废弃测试表） |
| 17 | 待拿取快递物资 | 7 | — | 仅数据中心可达 |
| 18 | 南大红会 志愿服务平台_个人主页（26年备份） | 21 | — | 仅数据中心可达 |
| 19 | 工作证明 | 5 | — | 仅数据中心可达 |

> 表中「—」表示该表未被平台业务模块读取，行数不影响平台运行，故未统计。

### 2.2 志愿服务 Base（**只读，绝无写入**）

- `dtable_uuid`：`6be54741-fbd0-4e16-9c66-cda281a5d899`
- Token 名称：`Web`（对应 `.env` 的 `SEATABLE_VOLUNTEER_API_TOKEN`）
- **共 23 张表**，平台读取其中 6 张。

| 表名 | 列数 | 平台读取的列 | 用途 |
| --- | ---: | --- | --- |
| 活动报名总表 | 27 | 活动名称、是否报名成功、报名结果、姓名 | 志愿活动报名量统计 |
| 活动签到 | 8 | 活动名称、姓名、活动时间、创建时间、已核对并录入 | 现场签到与核验记录 |
| 登记审批 | 16 | 活动名称、活动类别、活动日期、负责人、审批通过、进程 | 审批队列 |
| 个人主页（编辑版） | 27 | 仅统计行数 | 成员档案总量 |
| 活动及时长汇总表 | 7 | 活动名称、姓名、志愿时长、审核状态/状态/时长状态 | 时长待核对队列 |
| 报名通知 | 8 | 活动类别、活动名称、审批进程、隐藏、QQ群号 | 宣传中心的对外通知列表 |

其余 17 张表（问题反馈、体验反馈、志愿时长录入excel生成、献血车系列、留会统计、权限考核收集、Users 等）**未被平台读取**。

### 2.3 平台状态层的读写设计

状态层已全部迁入 SeaTable（见 §2.1 的 6 张状态表）。这里记录改造后的读写契约，因为它与一般业务表的行为**有意不同**：

| 规则 | 说明 |
| --- | --- |
| **读失败必须抛错** | `stateRows()` 不做 `try/catch` 静默降级。既然 SeaTable 是唯一真源，表不可达就必须呈现为错误状态，而不是「没有记录」——后者会让运营同学以为队列是空的。 |
| **审计是唯一例外** | `recordAudit()` 吞掉所有异常并只打 `console.error`。记录本身不应阻断它所描述的业务操作。 |
| **按行 upsert，不做整表覆盖** | 旧的 `读→改→写回` 整文件模式已删除。审核结论、排期任务、同意记录都按自然键定位单行后 `updateRow`：<br>· 审核结论与排期任务 → `投稿ID`<br>· 温暖连接参加 → `登记ID`（控制台为 `<用户名>:<项目>`，公众端为 `WARM-…`）<br>· 投稿与审计 → 直接 `appendRow` |
| **显示顺序由服务端显式排序** | `listRows` 返回的是插入顺序，不等于展示顺序。所有原本依赖数组顺序的列表改为按时间字段降序排序（`byDateDesc`）。 |
| **审核状态用统一词表** | `待审核` / `已通过` / `需修改` 三个值贯穿公众投稿、遗留内容审核与温暖连接投稿三条链路，`reviewDecisionFromStatus()` 负责与 API 的 `approve` / `return` 互转。 |

> ℹ️ 服务进程因此不再持有业务状态。多实例部署可以并行运行而不会互相覆盖数据 —— 这正是本次改造要解决的问题。
> 仍需注意：`操作审计表` 会随每次写操作增长，属于**增长最快**的一张表。当前量级（个位数行）无影响，但上线后应按季度归档。

> ⚠️ 遗留问题（不属于本次改造范围）：`工位物资表` 的读取上限、`是否报名成功` 的布尔列判定、以及库存预警阈值语义仍在 §5 中列出，尚未修复。

### 2.4 账号与角色配置

账号**不在 SeaTable 里**，而是服务端的配置文件，因为它是凭据而非业务数据：

- 文件：`.platform-accounts.json`（**已在 `.gitignore` 中**，口令为明文，绝不能提交）
- 环境变量：`PLATFORM_ACCOUNTS_FILE` 指向该文件；旧名 `PLATFORM_ADMIN_ACCOUNTS_FILE` 仍兼容，避免升级后起不来
- 单账号兜底：文件为空时退回 `PLATFORM_ADMIN_USERNAME` + `PLATFORM_ADMIN_PASSWORD`，创建一个 `platform_admin`

文件是一个 JSON 数组，每项 `{ username, password, role, label }`：

```json
[
  { "username": "admin1", "password": "…", "role": "platform_admin", "label": "管理平台管理员 1" },
  { "username": "user1",  "password": "…", "role": "member",         "label": "活动平台成员 1" }
]
```

启动时逐项校验：用户名唯一、口令长度达标、`role` 必须是 §1.5 表中的两个值之一。**任一项不合法即拒绝启动**，不做部分加载——半套账号上线比启动失败更难排查。

> 早期版本要求所有账号口令 ≥ 16 位且角色只能是 `platform_admin`。放开为双角色 + 分环境口令下限后，这个文件承载的不再只是管理员，因此从 `.admin-accounts.json` 更名为 `.platform-accounts.json`。

---

## 3. 功能与数据的对应关系

### 3.1 公众服务门户（浏览公开，提交与个人中心需登录）

| 页面 | 路由 | 前端调用的接口 | 数据来源 |
| --- | --- | --- | --- |
| 首页 | `/` | `GET /api/public/overview` | 读 活动项目/场次/报名表 + 工位物资表(计数) + 志愿 Base 活动报名总表(计数) |
| 活动广场 | `/events` | `GET /api/public/events` | 读 活动项目表、活动场次表、活动报名表 |
| 活动详情 | `/events/:eventId` | `GET /api/public/events/:eventId` | 同上 |
| — 报名抽屉 | — | `POST /api/public/events/:id/registrations` 🔒 | 读三表做容量/重复/候补裁决 → **写 活动报名表**（`参与者引用`=账号） + 审计 |
| 物资借用 | `/materials` | `POST /api/public/materials/requests` 🔒 | **写 物资管理**（状态=待审批） + 审计 |
| 内容投稿 | `/submit` | `POST /api/public/submissions` 🔒 | **写 宣传投稿表**（来源=公众投稿，`投稿人引用`=账号） + 审计 |
| 温暖连接 | `/warmth` | `POST /api/public/warmth/interest` 🔒 | **写 温暖连接参加表**（来源=公众端，`参与者标识`=账号） + 审计 |
| 我的状态 | `/status` | `POST /api/public/registrations/lookup` 🔒 | 读 活动报名表 + 活动项目表 + 活动场次表；仅返回本账号记录，或编号 + 邮箱双要素匹配的记录 |
| 活动平台登录 | `/login` | `POST /api/auth/login` | 账号与角色见 §2.4 |
| 个人中心 | `/me` | `GET /api/portal/me` 🔒 | 按账号聚合 活动报名表 + 宣传投稿表 + 温暖连接参加表 |
| 平台与隐私说明 | `/about` | 无 | 静态内容 |

🔒 = 需要登录活动平台（任意角色）。**未登录时这些页面不渲染表单**，而是给出登录入口，避免访客填完长表单才在提交时被拒。

> 个人中心**不接受任何调用方传入的标识**：服务端直接用会话用户名去筛选 `参与者引用` / `投稿人引用` / `参与者标识`，因此不存在越权读取他人记录的接口面。
> 已知缺口：`物资管理` 没有账号列（表结构归属物资模块），因此「我的物资借用」不在个人中心内，页面对此有明确说明并指向原有查询路径。

**公众端写接口的限流额度**（每小时 / 每 IP）：

| 动作 | 额度 | 附加条件 |
| --- | ---: | --- |
| 活动报名 | 6 | 登录 + 邮箱域名白名单 + 必须确认授权 |
| 物资借用 | 5 | 登录 + 邮箱域名白名单 + 必须确认责任条款 |
| 内容投稿 | 8 | 登录 + 邮箱域名白名单 + 原创确认 + 使用范围确认 |
| 温暖连接登记 | 5 | 登录 + 邮箱域名白名单 + 自愿参加/可退出确认 |
| 报名状态查询 | 30 | 登录；本账号记录只需编号，非本账号需「编号 + 邮箱」双要素 |
| 其他默认 | 12 | `PUBLIC_WRITE_LIMIT_PER_HOUR` |

**活动对公众可见的判定**（`isPubliclyListed`）：状态 ∈ {报名中, 进行中, 已结束} **且** 公开范围不含 {不公开, 仅管理员, 内部限定}。

### 3.2 运营管理控制台（需 `platform_admin`）

| 页面 | 路由 | 接口前缀 | 数据来源 |
| --- | --- | --- | --- |
| 工作台 | `/console/overview` | `GET /api/notifications/overview` | 跨模块聚合（见下） |
| 物资中心 | `/console/materials` | `/api/materials/*` | 物资管理、工位物资表、物资配置表、物资流水表 |
| 活动中心 | `/console/events` | `/api/events/*` | 活动项目表、活动场次表、活动报名表 |
| 志愿服务 | `/console/volunteers` | `GET /api/volunteer/overview` | 志愿 Base 5 张表（只读） |
| 宣传中心 | `/console/outreach` | `/api/outreach/*` | 3 张遗留宣传表 + 志愿 Base 报名通知 + **宣传投稿表** + **宣传发布任务表** |
| 温暖连接 | `/console/community` | `/api/community/*` | **温暖连接参加表** + **温暖连接投稿表** |
| 数据中心 | `/console/data` | `GET /api/health`、`/api/rows*` | 主 Base **全部 25 张表**（通用 CRUD） |
| 系统设置 | `/console/settings` | `/api/health`、`GET /api/audit/recent`、`/api/state/schema-preview` | Base 元数据 + **操作审计表** + 状态表结构预览 |

**工作台待办队列的聚合来源**（`getNotificationsOverview`，`Promise.allSettled`，任一源失败不影响其他）：

| 待办类型 | 优先级 | 来自 |
| --- | --- | --- |
| 物资逾期 · 库存预警 | high | 主 Base 物资管理 / 工位物资表+配置表 |
| 公众投稿待审核 | high | 主 Base 宣传投稿表（来源=公众投稿） |
| 物资审批 | medium | 主 Base 物资管理 |
| 活动待发布 | medium | 主 Base 活动项目表（状态=草稿） |
| 时长待核对 | medium | 志愿 Base 活动及时长汇总表 |
| 内容待发布 | medium | 主 Base 宣传发布任务表 |
| 温暖连接待审核 / 待确认 | medium | 主 Base 温暖连接投稿表 / 温暖连接参加表 |
| 内容待审核 | low | 主 Base 3 张遗留宣传表 |

### 3.3 模块 × 数据表 总览矩阵

`W` = 写入，`R` = 读取，`·` = 不涉及

| 数据表 | 公众端报名 | 公众端物资 | 公众端投稿 | 公众端温暖 | 控制台物资 | 控制台活动 | 控制台志愿 | 控制台宣传 | 控制台温暖 | 数据中心 |
| --- | :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-: |
| 活动项目表 | R | · | · | · | · | R W | · | · | · | R W |
| 活动场次表 | R | · | · | · | · | R W | · | · | · | R W |
| 活动报名表 | R W | · | · | · | · | R W | · | · | · | R W |
| 物资管理 | · | W | · | · | R W | · | · | · | · | R W |
| 工位物资表 | · | · | · | · | R | · | · | · | · | R W |
| 物资配置表 | · | · | · | · | R | · | · | · | · | R W |
| 物资流水表 | · | · | · | · | R W | · | · | · | · | R W |
| 博爱青春策划案 线下答辩 | · | · | · | · | · | · | · | R | · | R W |
| 博爱青春纪念品大赛 | · | · | · | · | · | · | · | R | · | R W |
| “红十字生命教育＋”第一轮试课 | · | · | · | · | · | · | · | R | · | R W |
| **宣传投稿表** | · | · | **W** | · | · | · | · | **R W** | · | R W |
| **宣传发布任务表** | · | · | · | · | · | · | · | **R W** | · | R W |
| **温暖连接参加表** | · | · | · | **W** | · | · | · | · | **R W** | R W |
| **温暖连接投稿表** | · | · | · | · | · | · | · | · | **R W** | R W |
| **操作审计表** | W | W | W | W | W | W | · | W | W | R W |
| 志愿 Base · 活动报名总表 | · | · | · | · | · | · | R | · | · | · |
| 志愿 Base · 活动签到 | · | · | · | · | · | · | R | · | · | · |
| 志愿 Base · 登记审批 | · | · | · | · | · | · | R | · | · | · |
| 志愿 Base · 个人主页（编辑版） | · | · | · | · | · | · | R | · | · | · |
| 志愿 Base · 活动及时长汇总表 | · | · | · | · | · | · | R | · | · | · |
| 志愿 Base · 报名通知 | · | · | · | · | · | · | · | R | · | · |

> 加粗的 5 行是本次新增的存储位置（第 6 张「宣传项目表」当前无写入方，仅作为征集窗口的预留结构）。
> `操作审计表` 的 `W` 覆盖所有会写数据的入口——每一笔写入都会同时留下审计记录。
> 数据中心的 `W` 是**表级放开**：解锁后可对任意表增删改（删除需二次输入 `DELETE`），不经业务状态机校验。

---

## 4. 关键表的字段级映射

### 4.1 物资模块（3 表 + 1 台账）

**关联键：`物资配置表.资产编码` = `物资流水表.资产编码` = `NJU-RC-<工位物资表行 _id>`**

实测关联完整性：工位物资表 140 行中 **100 行有配置记录**，配置表 100 条**无孤立记录**；缺口正好是平台读不到的那 40 行（见 §5.1）。

**`工位物资表`（只读，11 列）**

| 列名 | 类型 | 平台用法 |
| --- | --- | --- |
| 中心 / 物资名称 / 单位 / 柜号 / 层数 | single-select / text | 展示 |
| 初始数量 | number | 基准值 |
| **现有数量** | **formula** | 作为只读基线，**平台不改它** |
| 借出数量 / 归还数量 | number | 计算「历史未归还」 |
| 物资管理 | link | 仅记录来源状态 |

库存量计算规则：`平台数量 = 现有数量(formula) + Σ 物资流水表.数量delta`，其中 `入库/归还/盘点增加` 为 `+数量`，`出库/报损/盘点减少` 为 `-数量`。

**`物资配置表`（只读，9 列）**：平台只用 `资产编码`、`预警阈值`、`阈值启用`。阈值规则 = `阈值启用 === false ? 无预警 : (预警阈值 || max(3, 初始数量 × 20%))`。
`物资名称 / 中心 / 柜号 / 层数 / 最后盘点时间 / 备注` 6 列平台未使用。

**`物资管理`（读写，23 列）**

| 列名 | 类型 | 平台读 | 平台写 | 写入时机 |
| --- | --- | :-: | :-: | --- |
| 姓名 / 学号 / 邮箱 | text | ✓ | ✓ | 借用申请 |
| 借用用途 / 借用物资名及数量 | text / long-text | ✓ | ✓ | 借用申请 |
| 借用件数 | number | ✓ | ✓ | 借用申请 |
| 拟借用日期 / 拟归还日期 | date | ✓ | ✓ | 借用申请 |
| 状态 | single-select | ✓ | ✓ | 申请=`待审批`；审批通过=`开始`；出库=`借出（物资）`；归还=`已归还` |
| 借出审批 | single-select | ✓ | ✓ | 审批=`审批通过` / `审批不通过` |
| 不通过理由 | text | ✓ | ✓ | 审批拒绝 |
| 实际借用日期 | date | ✓ | ✓ | 出库 |
| 归还件数 | number | ✓ | ✓ | 归还 |
| 归还状态 | single-select | ✓ | ✓ | 归还=`已全部归还` / `物品缺失/数量减少` |
| 实际归还日期 | date | ✓ | ✓ | 全部归还时 |
| 物资出库照片 | image | ✓ | ✓ | 出库（**必填**） |
| 物资归还照片 | image | ✓ | ✓ | 归还（选填） |
| 审核人 / 部门 / 可借用物资 / 归还物资名及数量 / 马甲出库 | collaborator / single-select / link / text / formula | ✗ | ✗ | **平台未使用这 5 列** |

> 照片上传走 SeaTable 的 `app-upload-link` 接口，由服务端中转，文件名随机化，限制 JPG/PNG/WebP、单请求 8 MB。

**`物资流水表`（写入，14 列）—— 平台 100% 独占写入，当前 0 行**

| 列名 | 写入内容 |
| --- | --- |
| 流水编号 | `TX-<14位十六进制>` / 逾期提醒用 `REM-<16位>` |
| 申请单ID | 关联 `物资管理._id` |
| 资产编码 | `NJU-RC-<行ID>` |
| 物资名称 | 快照 |
| 操作类型 | `入库` / `出库` / `归还` / `盘点增加` / `盘点减少` / `报损` / `逾期提醒` |
| 数量 | 正整数 |
| 操作前数量 / 操作后数量 | 出库前预览 `18 → 15` 的数据来源 |
| 操作人 | 会话用户名（逾期提醒为 `system-reminder`） |
| 操作时间 | Asia/Shanghai 当日 |
| 异常说明 | 用户备注；或在「申请状态同步失败」时标记 |
| 流转去向 | 手工填写，或自动生成「申请人 · 借用用途」 |
| 损耗数量 | 归还时 `0 ≤ 损耗 ≤ 数量` |
| 幂等键 | 防重复提交；逾期提醒用 `OVERDUE:<申请ID>:<日期>` |

### 4.2 活动模块（3 表）

三张表的**结构已 100% 落地**（实测 14/14、9/9、15/15 列全部匹配）。

**`活动项目表`**（14 列，全部由 `POST /api/events` 写入）：
`活动ID`(`EVT-…`)、`活动名称`、`活动类型`、`活动简介`、`校区`、`地点`、`报名开始`、`报名截止`、`活动开始`、`活动结束`、`容量`、`负责人`、`状态`、`公开范围`
→ 新建时 `状态=草稿`；发布改 `报名中`，关闭改 `已结束`。

> 注意：`容量`、`报名开始/截止`、`活动开始/结束` 在该表中都是 **text 类型**，平台写入时做了 `String()` 转换，排序与比较均在应用层完成，依赖字符串字典序。

**`活动场次表`**（9 列）：`场次ID`(`SES-…`)、`活动ID`、`开始时间`、`结束时间`、`地点`、`容量`、`签到开放`、`签到方式`(固定 `二维码+人工核验`)、`状态=待发布`。

**`活动报名表`**（15 列）：

| 列名 | 写入内容 |
| --- | --- |
| 报名ID | `REG-…`（对外即「报名编号」，查询双要素之一） |
| 活动ID / 场次ID | 关联 |
| 参与者引用 | 公众端固定 `public-portal`；控制台为操作人用户名 |
| 显示姓名 / 南大邮箱 | 报名人信息 |
| 校区 | 报名填写，或回退场次/活动地点 |
| 报名答案 | `JSON.stringify({ note })` |
| 同意版本 | `v1` |
| 报名状态 | `已确认` / `候补` / `已取消` / `已签到` |
| 候补序号 | 候补时按已有候补数 +1 |
| **签到码摘要** | `sha256("<报名ID>.<签到码>")` 十六进制 —— **只存摘要，不存明文** |
| 提交时间 / 取消时间 / 签到时间 | ISO 时间戳 |

报名裁决全部在服务端：容量以场次容量优先、回退活动容量；`已确认 < 容量` 则确认，否则候补；同一活动内**邮箱重复即拒**。

### 4.3 志愿服务模块（6 表，全部只读）

| 表 | 平台读取的列 | 说明 |
| --- | --- | --- |
| 活动报名总表 | 活动名称、是否报名成功、报名结果、姓名 | 按活动名聚合报名量 |
| 活动签到 | 活动名称、姓名、活动时间、创建时间、已核对并录入 | 签到流水 |
| 登记审批 | 活动名称、活动类别、活动日期、负责人、审批通过、进程 | 取最后 8 条 |
| 个人主页（编辑版） | 仅行数 | 成员档案总量 |
| 活动及时长汇总表 | 活动名称、姓名、志愿时长、审核状态/状态/时长状态 | 取最后 12 条 |
| 报名通知 | 活动类别、活动名称、审批进程、隐藏、QQ群号 | 供宣传中心，取最后 12 条 |

### 4.4 宣传模块（3 张遗留表）

正式设计的 `宣传项目表` / `宣传投稿表` / `宣传发布任务表` **尚未建立**（实测 0/10、0/14、0/10 列匹配，三表均不存在）。运行时改为读取 3 张既有业务表，字段兼容性问题见 §5.3。

| 来源表 | 平台读取的列 | 实际存在的列 |
| --- | --- | --- |
| 博爱青春策划案 线下答辩 | 策划案名称、团队名称、负责人、团队负责人、姓名、提交时间、创建时间、策划案简介、项目简介、策划案内容、授权、是否同意公开 | 团队名称、序号、答辩人姓名、其他成员及对应分工、策划案名称、策划案PDF、答辩PPT、指导人 |
| 博爱青春纪念品大赛 | 文创名称、参赛类别、作者、姓名、负责人、提交时间、创建时间、作品简介、设计理念、参赛说明、授权、是否同意公开 | 学号、姓名、邮箱、参赛类别、团队其他成员姓名、文创名称、预期单价、文创用途、示例图片 |
| “红十字生命教育＋”第一轮试课 | 课程名称、改进建议、反馈人、姓名、提交时间、创建时间、课程反馈 | 课程名称、授课人、学号、课程分类、课程亮点、改进建议、反馈人、云录制链接、创建时间 |

这 3 张表**只读**，平台从不回写。审核结论与排期任务改写入 `宣传投稿表` / `宣传发布任务表`，并以 `投稿ID`（即 `planning:<行ID>` / `creative:<行ID>` / `feedback:<行ID>`）与遗留表行关联。公众端投稿也落在 `宣传投稿表`，靠 `来源` 列区分（见 §2.1）。

> 注意：审核遗留内容时会向 `宣传投稿表` upsert 一行并快照当时的标题，因此该表同时包含「真实投稿」与「审核记录」两类行。这是为了只建一张表而做的合并，查询时必须用 `来源` 过滤。

---

## 5. 实测数据现状与已发现的问题

### 5.1 ⚠️ 库存只覆盖前 100 行，40 件物资对平台完全不可见

`工位物资表` 真实 **140 行**，但 `getMaterialsOverview()`、`materialBundle()`、库存二维码接口、扫码接口全部使用 `listRows(..., 100)`。

后果：
- 库存总览统计的是前 100 行的数据：初始合计 **3782**、现有合计 **3580**；全量真值为 **5249 / 5047**（差 202 件两者一致）。
- **第 101–140 行物资无法出现在总览、无法生成二维码、无法被扫码定位。**
- 这 40 行恰好也是 `物资配置表` 中没有配置记录的那 40 行（配置表按前 100 行初始化）——两个 100 的上限互相印证。

### 5.2 ⚠️ 物资流水表为空，新流程尚未真正启用

`物资流水表` **0 行**。也就是说：
- 全部库存变动目前仍由旧汇总列（`借出数量` / `归还数量` / `现有数量` 公式列）承载，**没有任何一笔逐笔事件被记录**。
- 平台的「台账差值」统计（`totalUntrackedDifference = 0`）说明 202 件的差额都能被旧的「借出 − 归还」汇总解释，暂时没有出现无法追溯的缺口。
- 但「趋势图（入库/出库逐日序列）」「流转去向」「损耗统计」「幂等防重」这些依赖流水的能力，**因为没有数据而全部处于空状态**。

### 5.3 ⚠️ 宣传中心的作者与授权字段全部取不到

实测 40 条内容（12 策划案 + 18 文创 + 10 课程反馈）：
- `author` **全部为「未填写」** —— 代码读的是 `负责人 / 团队负责人 / 姓名`，而策划案表里实际叫 `答辩人姓名`、文创表里叫 `姓名`。
- `authorization` **全部为「待核对」** —— 代码读 `授权 / 是否同意公开`，**两列在 3 张表中都不存在**。这意味着平台把「授权」当作审核依据展示，却**无法从数据源判断内容是否已获授权公开**。
- `submittedAt` 实际取自 SeaTable 的 `_mtime`（行修改时间），不是真实投稿时间——两张表都没有 `提交时间` / `创建时间` 列。
- 审核状态：40 条全部「待审核」，0 通过、0 退回。

### 5.4 ⚠️ 志愿服务看板的「已确认」恒为 0

`活动报名总表.是否报名成功` 是 **checkbox 布尔列**（实测取值 `true` / `false`），而代码用正则 `/成功|通过|已报名/` 去匹配 `String(true)` → `"true"`，**永远匹配不上**。

结果：志愿服务看板上每个活动的 `confirmed` 都是 **0**（实测 20 个活动全部为 0），只有 `registrations` 是正确的。

### 5.5 ⚠️ 时长待核对队列的状态是合成的

`活动及时长汇总表` **没有** `审核状态` / `状态` / `时长状态` 任何一列，代码回退为常量字符串 `'待核对'`；而过滤逻辑恰好是「排除含 通过/已完成/已核对/已发放 的值」——常量 `'待核对'` 永远通过过滤。

结果：该队列**永远取最后 12 行**，与真实核对状态无关。抽样显示 `志愿时长` 多为 0（该表本身疑似未填充）。

### 5.6 ⚠️ 志愿 Base 读取已被上限截断

| 表 | 平台 limit | 实测返回 | 判断 |
| --- | ---: | ---: | --- |
| 个人主页（编辑版） | 1000 | 1000 | **恰好等于上限，已截断** |
| 活动报名总表 | 500 | 500 | **恰好等于上限，已截断** |
| 报名通知 | 100 | 100 | **恰好等于上限，已截断** |
| 活动签到 | 500 | 251 | 未截断 |
| 登记审批 | 500 | 26 | 未截断 |

所以看板上「成员档案 1000 人」「报名 500 条」是**读取上限**而非真实总量，界面上未作说明。

### 5.7 ⚠️ 库存预警大量误报

实测 **63 / 100** 条触发低库存预警，但抽检发现主要是阈值语义问题：`物资配置表` 的 `预警阈值` 按 `max(3, 初始数量 × 20%)` 批量初始化后，对**小额物资**阈值被抬到 3，反而高于其全部库存。

例：`真空机` 初始 2 / 现有 2 / 阈值 3 → 触发预警；`鼠标垫` 初始 1 / 现有 1 / 阈值 3 → 触发预警。这些物资**从未发生借出**，却持续出现在高优先级待办里，淹没了真实风险。

### 5.8 ✅ 已修复：本地 JSON 状态层（原架构最脆弱的一环）

**原问题**：投稿、温暖连接、审核结论、排期任务与审计全部落在 `logs/*.json`：
- 整表读写（读→改→写回），并发写会互相覆盖；多进程/多实例部署必然丢数据。
- 目录被 `.gitignore` 排除 → 无版本控制、无备份、无迁移路径。
- 与 SeaTable 中的数据没有事务关系：文件一旦损坏，整条链路不可恢复。

**修复方式**：新建 6 张 SeaTable 表（`宣传项目表`、`宣传投稿表`、`宣传发布任务表`、`温暖连接参加表`、`温暖连接投稿表`、`操作审计表`），删除服务端全部 `fs` 状态读写，改为按行 upsert / append。详见 §2.1 与 §2.3。

**验证**：`npm run check` 通过；`/api/state/schema-preview` 6 张表全部 `exists: true` 且字段 0 缺失；服务端已无 `appendFile` / `writeFile` / `mkdir` 任何调用。

**遗留注意**：`操作审计表` 会随写操作持续增长，是最需要归档策略的一张表。

### 5.9 ⚠️ 活动模块目前是空壳

`活动项目表` 仅 **1 行**（草稿），`活动场次表` **0 行**，`活动报名表` **0 行**。

意味着：结构已 100% 就绪，但**公众端「活动广场」当前没有可报名活动**，报名→候补→签到→二维码这条链路的全部分支（含候补序号、签到码校验）都尚未经过真实数据验证。

### 5.10 ⚠️ 数据中心对敏感表表级放开

「数据中心」可访问主 Base 全部 **25 张表**（19 业务表 + 6 状态表），包含 `换届报名表`（28 列）、`工作证明`、`全体成员表` 等含个人信息的表。保护措施是：预览中对匹配 `/手机|电话|身份证|银行|微信|QQ|邮箱|学号|住址|地址|密码/` 的列**始终隐藏**，写入需解锁、删除需二次输入 `DELETE`。

但需注意：**脱敏只作用于"预览展示"，写入与删除是表级放开的**，解锁后可对任意表的任意行直接改删，且**不经业务状态机校验**。本次新增的 6 张状态表同样受此影响——意味着解开原始写入后，可以绕过审核状态机直接改 `宣传投稿表.审核状态`。上线前建议把状态表从数据中心的表清单中排除。

### 5.11 其他已知边界

- 库存锁 `withMaterialLock()` 与登录失败计数仍是**进程内内存结构**；多进程部署需集中式锁或共享存储。
- 「流水已写入但申请状态同步失败」目前只在 `异常说明` 里打标记，**没有自动重试或人工重放入口**。
- 账号口令仍为**明文比对**，且 `platform_admin` 内部**不再细分**——管理平台的所有账号权限相同，未按表/字段/动作拆分最小权限（如物资管理员与内容审核员应能做的事并不一样）。
- 门户写入已要求登录，但账号本身没有姓名/学号，报名与借用表单仍需手工填写身份信息，两者之间**没有做一致性校验**。
- 学校统一身份认证未接入（需校方提供 CAS/OAuth/OIDC 参数）。
- 生产 SMTP、HTTPS、反向代理、备份恢复演练、监控告警与 Token 轮换均未完成。

---

## 6. 未接入的底表（可扩展清单）

以下表**平台完全不读取**，是后续功能拓展时可直接复用的现成数据源：

**主 Base（9 张）**
`全体成员表（26~27学年）`、`博爱青春队长团`、`工位值班表`、`换届意向统计`、`换届报名表`、`8月5日测试`、`待拿取快递物资`、`南大红会 志愿服务平台_个人主页（26年备份）`、`工作证明`

**志愿服务 Base（17 张）**
`个人主页（26年备份）`、`问题反馈`、`体验反馈`、`志愿时长录入excel生成`、`献血车志愿时长excel文件生成`、`志愿服务平台 脚本运行日志`、`市血液献血车排班表（模板表）`、`市血液献血车排班表（汇总底表）`、`市血液献血车排班表（汇总底表）(copy)`、`南大红会 志愿服务平台 Users`、`南大红会 志愿服务平台 (苏州分部) Users`、`献血车问题反馈`、`献血车签到表`、`权限考核收集`、`留会统计`、`个人主页（编辑版）(8.4备份)`、`多病共防宣教项目签到表`

其中较有价值的候选：
- `工位值班表` → 排班看板
- `问题反馈` / `体验反馈` / `献血车问题反馈` → 意见反馈闭环（目前平台没有这个模块）
- `市血液献血车排班表（汇总底表）`（21 列）→ 献血车排班与时长统计
- `留会统计` / `权限考核收集` → 组织运营统计

---

## 7. 平台状态表的建表与维护

6 张状态表由 `scripts/apply-state-schema.mjs` 创建，安全模型与既有的活动表脚本完全一致：

```bash
npm run state:dry-run     # 只读检查：打印将创建的表与列，不写任何数据
npm run state:apply -- --apply --confirm=CREATE-NJU-RC-STATE-TABLES
```

安全约束：

| 约束 | 说明 |
| --- | --- |
| 默认只读 | 不传 `--apply` 时仅打印计划并退出 |
| 显式确认短语 | 必须同时传 `--apply --confirm=CREATE-NJU-RC-STATE-TABLES`，避免误触 |
| 已存在即拒绝 | 任一目标表已存在就整体拒绝执行，**绝不覆盖或部分改写线上表** |
| 建表后复核 | 脚本重新读取 metadata 并回报 `verifiedPresent`，创建失败会列出 `failed` 并以非零码退出 |
| 列类型 | 与既有活动表一致，全部使用 `text`（时间字段也存 ISO 字符串），保证排序与比较行为可预期 |
| 页面不触发建表 | 控制台只提供 dry-run 预览（`/api/state/schema-preview`），真实建表必须在服务器上手工执行 |

> 前提：`SEATABLE_API_TOKEN` 必须对该 Base 有建表权限（当前 Token 权限为 `rw`）。
> 如需为新环境部署，按 `activities → state` 的顺序执行两个脚本即可。

### 7.1 状态表的清理（状态层迁移的副作用）

状态层从本地文件搬到 SeaTable 之后，**端到端验证会写出真实数据行**——本地 JSON 可以直接删掉整个文件，SeaTable 不行。因此新增 `scripts/clean-test-rows.mjs` 作为配套清理工具：

```bash
npm run state:clean-preview    # 只读：列出所有含 VERIFY- 标记的测试行
npm run state:purge-preview    # 只读：列出 6 张状态表的全部行（清空前先核对）
```

两种模式与确认短语：

| 模式 | 触发 | 确认短语 | 作用 |
| --- | --- | --- | --- |
| marker（默认） | `--apply` | `DELETE-NJU-RC-TEST-ROWS` | 只删除文本字段中含标记前缀（默认 `VERIFY-`）的行 |
| purge | `--purge --apply` | `PURGE-NJU-RC-STATE-TABLES` | **清空 6 张状态表的全部行**，仅用于建表后的线上下线演练 |

> ⚠️ `--purge` 会删除状态表中的所有行，不区分来源。生产环境请只用 marker 模式；purge 仅在建表初期、表中只有验证数据时使用。
> 建议验证脚本给每一行都打上可检索的标记（如投稿标题、备注、昵称统一带 `VERIFY-<stamp>`），这样 marker 模式能完整回收。

---

## 附录 A：本次核对的依据

1. **代码通读**：`server.js` 全文；`public/app/main.js`、`core/api.js`、`console/shell.js`、`portal/shell.js`、`console/pages/data.js`、`console/pages/settings.js`；`scripts/*.mjs`。
2. **运行时接口实测**（本地服务 `localhost:3000`，使用本地管理员会话）：
   `/api/health`、`/api/events/overview`、`/api/materials/overview`、`/api/volunteer/overview`、`/api/outreach/overview`、`/api/notifications/overview`、`/api/outreach/public-submissions`、`/api/community/interests`、`/api/events/schema-preview`、`/api/outreach/schema-preview`、`/api/state/schema-preview`。
3. **SeaTable 直连校验**（绕过沙箱代理，仅只读 `getMetadata` / `listRows`）：
   主 Base 各表的列结构、`活动报名总表.是否报名成功` 取值分布、物资三表 `资产编码` 关联完整性、`活动及时长汇总表` 实际列名。
4. **本阶段的写操作**：§5.8 的修复过程中，通过 `scripts/apply-state-schema.mjs` 向主 Base **新增了 6 张空表**。除建表外，未修改任何既有表的行数据，也未删除任何内容。
5. **端到端验证与回收**：状态层改造完成后，在本地 `localhost:3100` 实例上跑通 17 项端到端检查（公众投稿 → 控制台审核、温暖连接登记 → 确认、控制台同意 → 投稿 → 审核、遗留内容审核 → 排期 → 发布失败重试、操作审计落表、工作台聚合），随后用 `scripts/clean-test-rows.mjs --purge` 回收了验证期间写入的全部 29 行，6 张状态表已归零。
6. **账号与鉴权改造的验证**：
   - `npm run smoke:auth`（`scripts/smoke-auth.mjs`）**18 项**通过：六个账号可登录且角色正确、错误口令被拒、成员读控制台接口得 `403 console_forbidden`、未登录得 `401 login_required`、缺少 CSRF 得 `403 csrf_failed`、未登录访问 `/api/portal/me` 被拒、成员经控制台数据接口写入被拒、成员可正常登出且会话失效。
   - 同一脚本加 `--write` 后 **22 项**通过，额外证明「投稿只出现在提交者自己的个人中心」（user1 与 user2 互相看不到对方记录），并在结束时回收了自己写入的 2 条投稿与 2 条审计行。
   - `npm run smoke:public` 仍然通过，确认门户的公开读路径未被鉴权改动破坏。
   - 全量相对导入图校验：37 个前端文件、193 条相对导入、0 处缺失。
   - 两个新页面（`/login`、`/me`）在 Node 中配合最小 DOM shim 对**真实接口**完成渲染，8 项断言通过（含成员登录后外壳不显示管理平台入口）。
   - 本次验证写入的状态表行已用 `scripts/clean-test-rows.mjs --purge` 全部回收，6 张状态表再次归零。

## 附录 B：相关文档

- [README.md](./README.md) — 快速上手、路由与 API 总览
- [DEVELOPMENT_PLAN.md](./DEVELOPMENT_PLAN.md) — 阶段性建设顺序与验收标准
- [MATERIALS_MODULE.md](./MATERIALS_MODULE.md) — 物资数据映射与状态机
- [MATERIALS_GAP_ANALYSIS.md](./MATERIALS_GAP_ANALYSIS.md) — 物资模块完成度与剩余工作
- [OUTREACH_COMMUNITY_DEVELOPMENT_PLAN.md](./OUTREACH_COMMUNITY_DEVELOPMENT_PLAN.md) — 宣传、活动与温暖连接方案
