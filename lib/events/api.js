/**
 * Event operations extensions: notice generation and njubox attachments.
 *
 * PLACEHOLDER — the implementation belongs to the events workstream.
 * Mounted on /api/event-notices/* and /api/event-attachments/*; must return
 * false for any path it does not own. Contract: .workbuddy/parallel-contract.md §3
 */
export async function eventsOpsRoutes() {
  return false;
}
