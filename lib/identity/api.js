/**
 * Registration / e-mail verification routes.
 *
 * PLACEHOLDER — the implementation belongs to the identity workstream.
 * Mounted on /api/auth/* ahead of the original authApi; must return false for
 * any path it does not own. Contract: .workbuddy/parallel-contract.md §2
 */
export async function identityRoutes() {
  return false;
}
