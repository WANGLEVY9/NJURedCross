# 物资模块：当前实现与下一步开发

## 已接入的真实 SeaTable 数据

| 业务能力 | SeaTable 表 | 使用字段 | 当前状态 |
|---|---|---|---|
| 借用申请与审批队列 | `物资管理` | 姓名、借用用途、借用物资名及数量、拟借用/归还日期、状态、借出审批 | 已读；可创建待审批申请 |
| 出库与归还追踪 | `物资管理` | 实际借用日期、实际归还日期、归还件数、归还状态、物资出库照片、物资归还照片 | 已读；已接入审批、出库、归还状态机和照片留痕 |
| 库存与位置 | `工位物资表` | 物资名称、初始数量、单位、柜号、层数、借出数量、归还数量、现有数量 | 已读；二维码已接入 |
| 库存策略配置 | `物资配置表` | 物资名称、资产编码、中心、柜号、层数、预警阈值、阈值启用、最后盘点时间、备注 | 已建立并初始化 100 条 |
| 出入库流水 | `物资流水表` | 流水编号、申请单 ID、资产编码、操作类型、数量、前后数量、操作人、操作时间、流转去向、损耗数量、异常说明、幂等键 | 已建立；当前 0 条 |

现有状态中已观察到 `待审批`、`借出（物资）`、`出库/消耗`、`已归还`、`审批通过`、`已全部归还` 和 `物品缺失/数量减少`。写入逻辑只能使用已经确认的状态值，不能凭空创建单选项。

### 现有物资统计核验

初始化前已从 `工位物资表` 读取并核对：100 条物资记录，初始数量合计 3782，借出数量合计 202，归还数量字段合计 0，现有数量合计 3580；其中 1 条现有数量为 0，100 条具备柜号或层数位置。两张新表没有覆盖这些统计，库存数量的事实来源仍是 `工位物资表`。

由于旧表没有逐笔出入库事件，不能将 202 件借出数量伪造为流水记录；`物资流水表` 从新系统第一次真实出库、归还或盘点操作开始记录。

## 工作流设计

```text
借用申请 → 待审批 → 审批通过 → 出库/借出 → 待归还 → 已归还
                          │                         └→ 逾期提醒
                          └→ 不通过（附原因）
```

当前已实现：

- 管理员物资工作台：待审批、借出中、逾期、低库存指标。
- 物资总览：统计物资种类、理论总量、当前可用数量和库存差额；差额区分已有台账状态与新系统逐笔流水，并展示可关联的申请人和借用用途。
- 新建申请表单：用户最终确认后才创建 `状态=待审批` 的真实记录。
- 平台侧库存建议阈值：`max(3, 初始数量 × 20%)`；仅用于提醒。
- 二维码 SVG 标签：仅编码不透明资产码，必须登录后才能生成和查询。
- 浏览器原生 `BarcodeDetector` 扫码器：用户点击后才请求摄像头；扫码枪也可直接输入资产码。
- 物资管理者操作卡：入库、出库、归还、审批通过/拒绝均使用业务表单，不开放原始 JSON。
- 入库/出库/归还提交前显示“物资、位置、数量、库存变化”预览，并要求二次确认。
- 借出出库必须上传 JPG、PNG 或 WebP 现场照片；归还支持上传照片并将损耗情况写入备注。
- 出库流水结构化保存流转去向；归还流水结构化保存损耗数量，备注继续保存文字核查说明。
- 归还数量不足或填写损耗备注时，申请会标记为 `物品缺失/数量减少`，进入“归还异常”关注指标。
- 归还的结清判断为“实际归还数量 + 损耗数量”达到借出数量；全量结清但有损耗时仍保留异常状态。
- 出库和归还写入 `物资流水表`；平台汇总会将新流水叠加到既有 `工位物资表` 的当前数量上。
- 同一资产编码的写操作在单个 Node 进程内串行执行，并在锁内重新读取库存快照；多进程部署仍需集中式锁或事务。
- 逾期邮件提醒由 SMTP 配置启用；每个申请每天使用幂等键最多发送一次。

## 管理者人机交互原则

- 先任务后数据：进入物资中心先看到待审批、逾期和低库存任务，而不是一张宽表。
- 扫码优先：管理者可以扫码或使用扫码枪定位资产，再确认位置和当前库存。
- 预览后写入：任何入库、出库、归还都先展示库存变化，例如 `12 → 9`，再执行确认。
- 防误操作：危险操作需要明确的二次确认；数量不足、未审批、状态不匹配会在服务端拦截。
- 及时反馈：成功后刷新看板并显示流水结果；失败时保留表单内容，不清空用户输入。
- 可追溯：每条流水带有操作人、操作时间、申请单 ID、幂等键和前后数量。
- 反馈闭环：看板底部展示最近流水，管理员可以立即核对本次操作是否已经写入以及库存如何变化。

## 出库与归还写入的实现顺序

1. 将五个管理员账户收紧为物资模块专属权限，禁止其他管理员访问申请人联系方式。
2. 配置审核人 SeaTable 协作者 ID 映射，才能安全写入 `审核人` 字段。
3. 建立“申请记录 ↔ 库存行”的显式关联校验；只从扫码得到的库存行发起出库。
4. 在同一服务端事务逻辑中更新申请状态、实际日期、借出/归还数量与审计日志；若 SeaTable 某一步失败，返回可恢复的待处理状态，避免静默扣减库存。
5. 归还时记录数量差异、照片与异常原因；对于“物品缺失/数量减少”自动转入异常队列，而不是直接标记完成。

当前已提供的业务 API：

- `POST /api/materials/transactions`：登记入库、出库、归还、盘点增加、盘点减少或报损。
- `POST /api/materials/applications/:id/approve`：审批通过。
- `POST /api/materials/applications/:id/reject`：拒绝并要求理由。
- `POST /api/materials/applications/:id/checkout`：校验审批后出库并写流水。
- `POST /api/materials/applications/:id/return`：登记归还、更新归还数量和完成状态。

借出照片通过 SeaTable 的临时上传链接上传后写入 `物资出库照片` image 列；归还照片写入已有的 `物资归还照片` image 列。未配置 SMTP 时，逾期提醒不会发送邮件。

这些写入接口已完成服务端字段、库存、状态和幂等校验；正式联调时应先指向测试申请和可回收库存记录。

## 二维码服务边界

- 二维码内容：`NJU-RC-<库存行ID>`，而不是姓名、手机号、库存数量或借用申请内容。
- 标签生成：服务端使用本地 `qrcode` 包输出 SVG；不使用公共二维码生成网站。
- 查询：`GET /api/materials/scan` 需要已登录 Cookie，未知或格式不正确的码被拒绝。
- 摄像头：仅在管理员点击“启用摄像头”后调用浏览器权限；视频帧不上传到服务器。
- 打印：下一步提供 A4 标签批量导出，并在扫码后显示“待出库 / 待归还 / 异常”操作卡。

## 必须在上线前完成的安全工作

- 将本地明文初始账户密码迁移为 `scrypt` 或 Argon2 哈希。
- 接入校方统一身份认证并实现表级、字段级、动作级最小权限。
- 将审计日志写入独立、受保护的存储，不与业务表相互覆盖。
- 对 API Token 轮换、SeaTable 写入失败、重复扫码、断网重试和并发库存扣减进行集成测试。
- 当前 `npm audit` 报告的高危项来自既有 `seatable-api` 的 `axios` 依赖；自动建议会降级 SDK，不能直接执行。应在测试环境验证兼容的上游 SDK/依赖修复后再升级。
