# 南京大学红十字会管理平台：后续开发计划

## 总目标

将当前“受本地管理员保护的 SeaTable 数据工作台”演进为可审计、最小权限、支持红会日常物资、成员、活动和值班协同的平台；SeaTable 保持为业务数据底座，API Token 始终只在服务端使用。

## 阶段 0：安全基线与账户治理（第 1 周）

- 立即在 SeaTable 撤销截图中出现过的 API Token，替换为新的最小权限 Token。
- 将 `.platform-accounts.json` 中的明文初始密码迁移为 `scrypt`/Argon2 密码哈希；生产环境将账户资料放入受保护数据库或校方身份提供方。
- 角色拆分已落地第一层：`platform_admin` / `member` 两类，控制台接口仅对前者开放，门户写入与个人中心要求登录。**仍需**把 `platform_admin` 细分为物资、成员、活动与值班、志愿服务等模块级角色，并落实到 API 的表级与动作级权限。
- 记录登录、读取敏感表、创建、修改、删除、导出和权限变更等审计事件（登录事件目前尚未写审计）。

验收：非授权角色无法请求不属于自己的表或执行写入；每一条写入都有操作者、时间、操作类型和前后摘要。

## 阶段 1：数据契约与业务读模型（第 2–3 周）

- 为各 SeaTable 表建立字段映射、必填项、枚举值、日期与关联字段校验，不再让普通业务人员提交原始 JSON。
- 建立只读业务 API：物资库存、借用单、成员目录、活动排期、值班排班、志愿服务记录。
- 定义敏感字段分级和服务端脱敏策略，避免仅依赖浏览器展示层。
- 对现有 14 张表盘点所有者、用途、个人信息字段与数据保留期限。

验收：每个已接入模块有字段字典、接口契约和错误提示；错误字段不会被写入 SeaTable。

## 阶段 2：核心业务闭环（第 4–6 周）

- 物资：申请、审批、出库、归还、逾期提醒、库存阈值预警。
- 成员：组织架构、任期、状态变更、最小化成员信息展示。
- 活动和值班：日历、报名、排班冲突检测、提醒与签到记录。
- 志愿服务：服务项目、时长登记、审核、个人服务档案与统计。

验收：每个流程有明确状态机、审批责任人、异常处理和可追溯操作记录。

## 阶段 3：校方身份认证与部署（第 7–8 周）

- 获得校方 CAS/OAuth/OIDC 接入参数、回调地址登记和身份属性说明后，实现统一登录适配器。
- 将账号角色映射至校方身份或受控白名单；保留紧急 `superadmin` 恢复机制。
- 经 HTTPS、反向代理、密钥托管、备份与恢复演练后部署到受控内网环境。
- 增加限流、健康检查、错误监控和告警。

验收：统一认证用户可按角色访问；登录、退出、会话过期、权限回收和故障恢复均经过测试。

## 阶段 4：质量与持续运营（持续）

- 为鉴权、权限、CSRF、字段校验和业务状态机补单元与接口测试。
- 使用测试表或可回收测试行完成 SeaTable 写入集成测试，严禁在真实业务表中做无标识试验。
- 每月审查 Token、账户、角色、审计日志和敏感数据留存；按学年进行角色交接与账户轮换。

验收：发布前自动完成代码检查、鉴权回归和测试数据 CRUD；生产变更具备审批、回滚和审计记录。
