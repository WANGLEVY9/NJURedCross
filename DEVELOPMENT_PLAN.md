# 南京大学红十字会平台：开发方向与优先级排期

> **依据**：2026-09-16 需求沟通截图（8 张）
> **术语**：**table** = njuTable，自部署 SeaTable（`table.nju.edu.cn`）；**box** = njuBox（`box.nju.edu.cn`）
> **上位约束**（来自沟通，贯穿全程）：
> 1. 数据尽量长期留在 **table 底表**；
> 2. **底表尽量不改动**，现有 SeaTable 侧自动化邮件保留；
> 3. 底表只对**最核心负责人**开放，其他同学只在**工作台**上操作。

---

## 一、需求还原

逐条对应截图，不改写、不合并，方便回头核对。

| # | 原始诉求 | 出处 | 当前状态 |
| --- | --- | --- | --- |
| **R1** | 创建活动也写入底表；「活动简介」等于原来的公告；按**固定框架**填时间、地点、内容，然后**自动生成一份报名通知** | 图1 | 建活动已可（落 `活动项目表`，草稿）；**无模板化生成** |
| **R2** | 策划案是跟 box 绑还是跟 table 绑？希望**能绑 box** | 图2 | **无任何 box 集成** |
| **R3** | 想**打通 box 与 table**：能引用 box 内容，但"好像没法写入 box"、"table 可以读，没法写" | 图3 | 无集成；**可行性已查明，见 §2.1** |
| **R4** | 担心"不加验证怎么保证报名信息只能看自己的"；能否给**每位同学唯一身份码**；注册能实现 | 图4 | 报名**按账号隔离已完成**；**无自助注册、无身份码** |
| **R5** | **交叉绑定**：用统一身份认证注册 → 邮箱-验证码-自设密码 → 登录新平台；后续都是邮箱+密码；**强绑定 @smail.nju.edu.cn** | 图5 | 无 SSO、无邮件验证码；邮箱域名白名单已有 |
| **R6** | 预留板块里的**早晚安短信**和我们一直想做的**匹配**；**消息订阅**功能 | 图6 | 早晚安/生日投稿与人工审核已有；**匹配只出候选统计**；**消息订阅不存在** |
| **R7** | 域名和服务器部署（对方评价"这个很麻烦"） | 图6 | 未开始 |
| **R8** | **双轨权限**：既用工作台做，也能直接通过 table 底表办理数据权限；底表只有核心负责人能看到，其他同学只在工作台操作 | 图6 | `platform_admin` 内部**未分权** |
| **R9** | 现在的**自动化邮件也是保留的**，等于底表不做修改 | 图7 | 物资逾期提醒邮件已实现（因 SMTP 未配置而处于关闭状态） |
| **R10** | 有机会在平台玩**线上 UNO** | 图8 | 彩蛋，backlog |

---

## 二、三个决定可行性的技术结论

这三条是本次规划前实测/查证得到的，直接改变了排期。

### 2.1 njubox 是 Seafile 企业版 —— **可以写入**

`box.nju.edu.cn` 是南京大学本地部署的 **Seafile 企业版**（实测版本 13.0.25，品牌名"南大云盘"）。它提供：

- **Seafile Web API**（`https://box.nju.edu.cn/api2/`）：获取 Token、列出资料库、上传 / 更新 / 下载文件、创建分享链接
- **WebDAV**：`https://box.nju.edu.cn/seafdav`（可用 rclone 直接挂载）
- **匿名探测**：`/api2/ping/`、`/api2/server-info/` 无需认证

> **结论：图3 里"没法写入 box"的顾虑不成立。** 写入是 Seafile 的标准能力（`getUploadLink` / `uploadFile` / `getUpdateLink`）。
> 真正的前置条件是**拿一个服务账号，或指定资料库的 API Token**。

**设计取向**：平台只存**文件引用**（资料库 ID / 路径 / 大小 / 校验和 / 分享链接），**文件本体留在 box**——和"数据留在底表"是同一个思路，平台不做第二份文件存储。

### 2.2 「报名通知」**已经存在**，但在只读的志愿 Base 里

志愿 Base（23 张表，代码中硬标记 `readOnly: true`，**绝无写入**）里已经有这些表：

| 表名 | 列数 | 平台目前如何用 |
| --- | ---: | --- |
| `报名通知` | 8 | 只读，供宣传中心通知列表（活动类别/活动名称/审批进程/隐藏/QQ群号） |
| `活动报名总表` | 27 | 只读，志愿报名量统计 |
| `活动及时长汇总表` | 7 | 只读，时长待核对队列 |
| `活动签到` | 8 | 只读，现场签到与核验 |
| `个人主页（编辑版）` | 27 | 只读，仅统计行数 |
| `Users` | — | **未被读取**（可能正是交叉绑定的落点） |

> **结论**：图1 说的"自动复制出来一份报名通知"正好对应这张**现成的表**，但平台目前**只读不写**。
> 这是 P1 的一个决策点（见 §五 Q2）：是绕开它在新 Base 另建一张通知表，还是申请写权限直接写它。

### 2.3 邮件通道已具备，但**尚未配置**

`nodemailer` 已接入 SMTP，目前**仅**用于物资逾期提醒；由于 `SMTP_HOST` / `SMTP_USER` / `SMTP_PASSWORD` 为空，发信整体处于关闭状态。

> **结论**：邮箱验证码（R4/R5）和消息订阅（R6）**共用同一条邮件通道**，应一次建成通用发信服务，避免做两遍。

---

## 三、开发方向：三主线 + 一支撑线

### 主线 A · 身份与信任（Identity）

让"每位同学有唯一身份"真正成立：自助注册 → 唯一身份码 → 邮箱验证 → 强绑定 smail。

**这是所有学生侧功能的前置**——报名、投稿、匹配、消息订阅、时长档案都建立在"知道这个人是谁"之上。也直接回答图4 的核心担忧。

### 主线 B · 活动运营闭环（Event Ops）

创建活动 → 自动生成报名通知 → 报名 → 签到 → 时长登记。把志愿 Base 里**已有的真实数据**接进闭环，而不是另起一套。

### 主线 C · 数据与文件打通（Data & Files）

table 存主数据、box 存文件、权限分层。落实 R2/R3/R8：策划案与附件进 box，平台存引用；核心负责人看底表，其他同学只用工作台。

### 支撑线 D · 通知通道（Messaging）

统一发信服务，一次建设三处受益：**验证码** + **消息订阅** + **逾期/变更提醒**。同时保住 R9 要求的现有自动化邮件。

---

## 四、优先级排期

工作量标记：**S** ≈ 半周内，**M** ≈ 1–2 周，**L** ≈ 2 周以上。周期为相对估算，外部依赖到手后才可固化。

### P0 · 地基（建议 2–3 周）

没有这一层，后面的学生侧功能都会返工。

| 编号 | 任务 | 现状 | 关键交付 | 工作量 |
| --- | --- | --- | --- | --- |
| **P0-1** | **账号迁移到 table 并改哈希** | `.platform-accounts.json` 明文配置、6 个写死账号 | 新建账号表（**新增，不动现有表**）；密码 `scrypt` 哈希；账号可增删改 | M |
| **P0-2** | **自助注册 + 每位同学唯一身份码** | 无注册；身份靠调用方传入 | 注册流程；签发不透明身份码（如 `RC-M-XXXXXXXX`，**不用学号**）；身份码即记录归属键 | M |
| **P0-3** | **邮箱验证 + 强绑定 @smail.nju.edu.cn** | 仅有域名白名单，无验证码 | smail 校验；一次性、有时效的验证码；同邮箱不可重复注册 | M |
| **P0-4** | **通用发信服务**（支撑线 D 第一块） | nodemailer 已接，仅逾期提醒，SMTP 未配置 | 模板化发信 + 幂等 + 收件人限频 + 发送留痕；验证码与后续订阅复用 | S–M |
| **P0-5** | **写入归属收口** | 报名/投稿已绑账号；**物资借用申请表没有账号列** | 所有学生侧写入可回溯到账号/身份码；补物资申请账号列（**新增列，不改旧列**） | S |

> **验收**：非 smail 邮箱无法注册；验证码不可复用、过期失效；任一报名/投稿/借用都能答出"谁提交的"；全链路无明文口令。

### P1 · 核心闭环（建议 3–5 周）

有身份之后立刻能产出价值的部分。

| 编号 | 任务 | 现状 | 关键交付 | 工作量 |
| --- | --- | --- | --- | --- |
| **P1-1** | **活动创建 → 自动生成报名通知** | 建活动已可（草稿），`活动简介` 字段在，**无模板生成** | 控制台填固定框架（名称/时间/地点/内容/容量/报名窗口）→ 服务端渲染标准通知文案 → 写入 `活动简介` | M |
| **P1-2** | **「报名通知」落地策略**（**需决策，见 Q2**） | 志愿 Base 的 `报名通知` 表**只读** | 方案 A：主 Base 新建通知表；方案 B：申请写权限直接写志愿 Base | S（A）/ M（B，含授权） |
| **P1-3** | **njubox 打通：策划案与附件** | 无任何 box 集成 | 上传活动策划案/宣传附件到指定资料库；平台存文件引用；前端可下载 | M |
| **P1-4** | **活动模块真实跑通** | 活动项目表 1 行草稿、场次 0、报名 0，链路未经真实数据验证 | 用真实活动跑通 创建→通知→报名→候补→签到 全链路 | M |

> **验收**：创建活动后通知文案可预览、可编辑、可复制；策划案上传→平台引用→点开可下载；一轮真实活动全流程走通且审计留痕。

### P2 · 运营增强（建议 4–6 周）

| 编号 | 任务 | 现状 | 关键交付 | 工作量 |
| --- | --- | --- | --- | --- |
| **P2-1** | **消息订阅** | `/api/notifications/overview` 只是控制台**待办聚合**，不是用户订阅 | 学生可订阅活动发布/物资/内容更新；站内 + 邮件；一键退订 | M |
| **P2-2** | **匹配：从 preview 到真实配对** | `/api/community/matching-preview` 只出候选统计，`pairs: []` | 早晚安/生日配对逻辑 + 人工确认 + 退出即失效 | L |
| **P2-3** | **权限分层：底表 / 工作台双轨** | `platform_admin` 内部不分权 | 模块级角色（物资/活动/内容/成员）；「核心负责人看底表、其他同学只用工作台」成文 | M |

> **验收**：订阅与退订即时生效且留痕；配对结果经人工确认后才产生联系；模块角色只能操作本模块 API。

### P3 · 部署与外部依赖（需外部输入才能启动）

| 编号 | 任务 | 阻塞点 |
| --- | --- | --- |
| **P3-1** | **统一身份认证（CAS）对接**（R5 前半段） | 需校方提供接入参数、回调地址登记、身份属性说明 |
| **P3-2** | **与"现在的志愿服务平台"交叉绑定** | 需确认旧平台**是否有导出/API**，以及绑定键（学号？邮箱？） |
| **P3-3** | **域名 + 服务器部署** | 需域名、服务器、HTTPS 证书、反向代理；对方已表示"很麻烦" |

### Backlog

- **B-1** 线上 UNO（R10）——彩蛋，不占主线资源。
- **B-2** 志愿时长档案与学生个人服务履历（若 P3-2 打通后可自然延伸）。
- **B-3** 现行 `listRows` 读取上限清理：`工位物资表` 实有 140 行但只读到 100 行，`个人主页（编辑版）`/`活动报名总表` 等读取上限已被顶满，界面数字是**上限而非真实总量**。

---

## 五、需要你决策的问题

| 编号 | 问题 | 为什么必须先定 |
| --- | --- | --- |
| **Q1** | **njubox 用哪种凭据？** ① 一个服务账号的 Web API Token（可跨资料库），还是 ② 指定资料库的 API Token（仅该库）？由谁提供？ | 决定 P1-3 能不能开工，以及权限边界 |
| **Q2** | **「报名通知」写在哪里？** 方案 A：主 Base 新建通知表（不碰志愿 Base）；方案 B：申请写权限直接写志愿 Base 的 `报名通知` | 决定 P1-2，也决定是否打破"志愿 Base 只读"这条现有硬约束 |
| **Q3** | **"交叉绑定"的准确含义？** 我的理解：统一身份认证只用于**注册/核身一次**，之后新平台完全用 **邮箱+密码** 登录，两者靠邮箱绑定。是这样吗？ | 决定 P3-1/P3-2 的架构；理解偏差会白做 |
| **Q4** | **邮箱验证码的 SMTP 由谁提供？** 校内邮箱服务还是第三方？能否发到 @smail.nju.edu.cn？ | 决定 P0-3/P0-4，**这是 P0 的硬前置** |
| **Q5** | **R8 的"直接操作底表"是运营制度还是产品功能？** 即：核心负责人直接用 SeaTable 界面（平台只需把工作台权限做小），还是平台里要另做一个直连底表的高权限入口？ | 决定 P2-3 的工作量差异很大 |

---

## 六、仍然有效的安全基线（沿用并补充）

这部分与需求无关，但**不能因为做新功能而丢**。

- 在 SeaTable 撤销一切曾出现在截图/聊天里的 API Token，换为最小权限 Token。
- 账号口令从明文迁移为 `scrypt`/Argon2（已并入 **P0-1**）。
- 登录事件目前**未写审计**，应补齐（登录、失败、锁定、改密、退订）。
- 敏感字段分级与服务端脱敏，不依赖浏览器展示层。
- 限流已覆盖公开写入；新增的注册、验证码、订阅接口**都要纳入限流**，验证码尤其要防刷。
- 发布前跑 `npm run check` + `npm run smoke:auth` + `npm run smoke:public`；SeaTable 写入测试只用带标记的可回收测试行，**严禁在真实业务表做无标识试验**。

---

## 附：建议的执行顺序（依赖视角）

```
P0-1 账号迁移 ─┬─ P0-2 注册+身份码 ─┬─ P0-5 写入归属收口
               │                     │
P0-4 发信服务 ─┴─ P0-3 邮箱验证 ─────┘
                          │
                          ├─ P1-1 活动通知生成 ── P1-2 通知落地（Q2）
                          ├─ P1-3 njubox 策划案（Q1）
                          └─ P2-1 消息订阅
                 P1-4 活动全链路真实跑通（依赖 P1-1/P1-2）
                 P2-2 匹配 / P2-3 权限分层
                 P3-* 外部依赖就绪后启动
```

**关键路径**：`P0-4 发信服务` → `P0-3 邮箱验证` → 学生侧功能全线解锁。
发信服务本身很小，但它是整条关键路径上的闸门，应最早动工。
